/* ============================================================
   SIMPLE TRAVEL — E-TICKET GENERATOR  v3.4
   app.js — Embedded Simple Travel Logo (No CORS Issues),
           Padded Canvas Capture (No Edge Touch),
           Editable & Updatable Saved LƯU Ý Templates (VI, EN, Bilingual, Custom)
   ============================================================ */
'use strict';

// ─── Constants ──────────────────────────────────────────────
const BRAND_DEFAULT = '#F5A623';
const TICKET_PRIMARIES = [
  '#0065e9','#0065E9','#f39c12','#F39C12',
  '#e9a100','#E9A100','#003d88',
];
const PRESETS = [
  { hex: '#F5A623', label: 'Simple Travel Orange' },
  { hex: '#D4891A', label: 'Simple Travel Dark' },
  { hex: '#0065E9', label: 'Xanh dương (gốc)' },
  { hex: '#E53E3E', label: 'Đỏ' },
  { hex: '#38A169', label: 'Xanh lá' },
  { hex: '#805AD5', label: 'Tím' },
  { hex: '#00B5D8', label: 'Ngọc' },
  { hex: '#1a1a2e', label: 'Xanh đen' },
];

// Embedded Simple Travel Logo Base64 (guarantees no CORS or missing logo issues)
const SIMPLE_TRAVEL_LOGO_BASE64 = 'data:image/png;base64,';

// Default built-in templates for LƯU Ý
const DEFAULT_LUUY_TEMPLATES = [
  {
    id: 'vi',
    name: '🇻🇳 Tiếng Việt (Chuẩn)',
    isDefault: true,
    content: `<p>-----</p>
<p><span style="color:#c0392b;">✈️<strong>LƯU Ý KHI LÀM THỦ TỤC BAY</strong></span></p>
<p>⏰ Có mặt tại sân bay: <strong>90 phút</strong> (Nội địa) | <strong>180 phút</strong> (Quốc tế).</p>
<p>📲 Khuyến nghị <strong>check-in online</strong> trước chuyến bay.</p>
<p><span style="color:#d35400;"><strong>GIẤY TỜ TÙY THÂN</strong></span></p>
<p>🌍 <strong>Hành khách quốc tịch nước ngoài:</strong> Hộ chiếu bản gốc còn hạn &amp; Visa (nếu yêu cầu).</p>
<p>🇻🇳 <strong>Hành khách Việt Nam:</strong> CCCD / Hộ chiếu / VNeID mức 2. Trẻ em: Giấy khai sinh bản gốc hoặc trích lục.</p>
<p>🤰 <strong>Hành khách mang thai:</strong> Vui lòng thông báo Đại lý trước khi xác nhận dịch vụ.</p>
<p>📞 <strong>Hỗ trợ:</strong> 0768.188.224 - <strong>Email:</strong> booking@thesimple.media</p>
<p style="color:#f39c12;text-align:center;">✨ Chúc Quý khách có chuyến bay an toàn &amp; thuận lợi ✨</p>`
  },
  {
    id: 'en',
    name: '🇬🇧 Tiếng Anh (English)',
    isDefault: true,
    content: `<p>-----</p>
<p><span style="color:#c0392b;">✈️<strong>IMPORTANT FLIGHT &amp; CHECK-IN NOTICES</strong></span></p>
<p>⏰ <strong>Airport arrival:</strong> at least <strong>90 mins</strong> (Domestic) | <strong>180 mins</strong> (International) prior to departure.</p>
<p>📲 <strong>Online check-in</strong> is recommended before arrival at airport.</p>
<p><span style="color:#d35400;"><strong>REQUIRED TRAVEL DOCUMENTS</strong></span></p>
<p>🌍 <strong>Foreign Citizens:</strong> Original valid Passport &amp; required Visas/Entry permits.</p>
<p>🇻🇳 <strong>Vietnamese Citizens:</strong> National ID (CCCD) / Passport / VNeID Level 2. Children: Original Birth Certificate.</p>
<p>🤰 <strong>Expectant Mothers:</strong> Please notify the booking agent prior to flight confirmation.</p>
<p>📞 <strong>24/7 Support:</strong> (+84) 0768.188.224 - <strong>Email:</strong> booking@thesimple.media</p>
<p style="color:#f39c12;text-align:center;">✨ Wish you a safe, smooth and pleasant flight ✨</p>`
  },
  {
    id: 'bilingual',
    name: '🌐 Song ngữ (VI & EN)',
    isDefault: true,
    content: `<p>-----</p>
<p><span style="color:#c0392b;">✈️<strong>LƯU Ý THỦ TỤC BAY / IMPORTANT NOTICES</strong></span></p>
<p>⏰ <strong>Giờ có mặt / Airport Arrival:</strong> 90 phút (Nội địa/Domestic) | 180 phút (Quốc tế/International).</p>
<p>📲 Khuyến nghị <strong>check-in online</strong> trước chuyến bay / Online check-in recommended.</p>
<p><span style="color:#d35400;"><strong>GIẤY TỜ / TRAVEL DOCUMENTS:</strong></span></p>
<p>🌍 <strong>Khách nước ngoài / Foreigners:</strong> Hộ chiếu còn hạn &amp; Visa / Valid Passport &amp; required Visa.</p>
<p>🇻🇳 <strong>Khách Việt Nam / Vietnamese:</strong> CCCD / Hộ chiếu / VNeID mức 2. Trẻ em: Giấy khai sinh.</p>
<p>📞 <strong>Hotline:</strong> 0768.188.224 - <strong>Email:</strong> booking@thesimple.media</p>
<p style="color:#f39c12;text-align:center;">✨ Chúc Quý khách chuyến bay tốt đẹp / Have a pleasant flight ✨</p>`
  }
];

const LS_KEY_TEMPLATES = 'st_eticket_luuy_templates_v3';

// ─── State ──────────────────────────────────────────────────
const state = {
  rawHTML: null,
  themeColor: BRAND_DEFAULT,
  useGradient: false,
  fontScale: 90,
  fontFamily: 'Inter',
  editMode: false,
  fileName: '',
  luuYVisible: true,
  luuYContent: null,
  luuYOriginal: null,
  activeTemplateId: null,
  airlineLogo: null,           // Custom logo (data URL) or null for original
  airlineName: null,           // Custom airline name text or null for original
  originalAirlineLogo: null,   // Extracted from original ticket HTML
  originalAirlineName: null,   // Extracted from original ticket HTML
  showA4QR: true,              // Show QR code badge on A4 ticket
  ticketData: null,            // Extracted ticket data for mobile & QR
  mobileUrl: '',               // Generated mobile URL with compressed data
};

// ─── DOM ────────────────────────────────────────────────────
const $ = id => document.getElementById(id);
const uploadZone            = $('upload-zone');
const fileInput             = $('file-input');
const fileInfo              = $('file-info');
const fileNameEl            = $('file-name');
const fileSizeEl            = $('file-size');
const btnRemove             = $('btn-remove');
const panelColors           = $('panel-colors');
const panelFont             = $('panel-font');
const panelAirline          = $('panel-airline');
const panelLuuY             = $('panel-luu-y');
const previewEmpty          = $('preview-empty');
const previewToolbar        = $('preview-toolbar');
const previewContainer      = $('preview-container');
const previewIframe         = $('preview-iframe');
const colorPicker           = $('color-picker');
const colorHex              = $('color-hex');
const gradientToggle        = $('gradient-toggle');
const btnReset              = $('btn-reset');
const fontSelect            = $('font-select');
const btnPDF                = $('btn-pdf');
const btnImg                = $('btn-img');
const btnCopy               = $('btn-copy-image');
const btnToggleEdit         = $('btn-toggle-edit');
const editToolbar           = $('edit-toolbar');
const editBanner            = $('editing-banner');
const fontSlider            = $('fontscale-slider');
const fontValue             = $('fontscale-value');
const btnFontDec            = $('fontscale-decrease');
const fontInc               = $('fontscale-increase');
const btnFontReset          = $('btn-fontscale-reset');
const loading               = $('preview-loading');
const toastCont             = $('toast-container');
const luuYToggle            = $('luu-y-toggle');
const luuYEditor            = $('luu-y-editor');
const btnLuuYApply          = $('btn-luu-y-apply');
const btnLuuYReset          = $('btn-luu-y-reset');
const luuYTemplateList      = $('luu-y-template-list');
const luuYNewTemplateName   = $('luu-y-new-template-name');
const btnLuuYAddTemplate    = $('btn-luu-y-add-template');
const btnLuuYUpdateTemplate = $('btn-luu-y-update-template');
const btnLuuYResetAllTpls   = $('btn-luu-y-reset-all-templates');

// Mobile & QR Modal DOM
const btnMobileQR           = $('btn-mobile-qr');
const modalMobileQR         = $('modal-mobile-qr');
const btnModalClose         = $('btn-modal-close');
const mobilePreviewIframe   = $('mobile-preview-iframe');
const qrCodeContainer       = $('qr-code-container');
const btnDownloadQR         = $('btn-download-qr');
const mobileUrlInput        = $('mobile-url-input');
const btnCopyMobileUrl      = $('btn-copy-mobile-url');
const btnDownloadMobileImg  = $('btn-download-mobile-img');
const btnCopyMobileImg      = $('btn-copy-mobile-img');
const btnCopyFlightSummary  = $('btn-copy-flight-summary');
const a4QRToggle            = $('a4-qr-toggle');

// History Modal DOM
const btnHeaderHistory      = $('btn-header-history');
const btnToolbarHistory     = $('btn-toolbar-history');
const modalHistory          = $('modal-history');
const btnModalHistoryClose  = $('btn-modal-history-close');
const historySearchInput    = $('history-search-input');
const btnClearAllHistory    = $('btn-clear-all-history');
const historyTableContainer = $('history-table-container');

// Airline panel DOM
const airlineLogoInput       = $('airline-logo-input');
const airlineUploadZone      = $('airline-upload-zone');
const airlineLogoPreviewRow  = $('airline-logo-preview-row');
const airlineLogoPreviewImg  = $('airline-logo-preview-img');
const airlineLogoNameDisplay = $('airline-logo-name-display');
const btnAirlineClear        = $('btn-airline-clear');
const airlineNameInput       = $('airline-name-input');
const btnAirlineApply        = $('btn-airline-apply');
const btnAirlineReset        = $('btn-airline-reset');
const btnEditToolbarAirline  = $('btn-edit-toolbar-airline');

// ─── Preset Color Buttons ───────────────────────────────────
function buildPresets() {
  const grid = $('preset-grid');
  PRESETS.forEach(p => {
    const btn = document.createElement('button');
    btn.className = 'preset-btn';
    btn.dataset.color = p.hex;
    btn.title = p.label;
    btn.style.setProperty('--preset-color', p.hex);
    btn.innerHTML = '<span class="preset-btn__check">✓</span>';
    btn.addEventListener('click', () => applyColor(p.hex));
    grid.appendChild(btn);
  });
  markActivePreset(state.themeColor);
}

function markActivePreset(hex) {
  document.querySelectorAll('.preset-btn').forEach(btn => {
    btn.classList.toggle('active', btn.dataset.color.toLowerCase() === hex.toLowerCase());
  });
}

// ─── Color Helpers ──────────────────────────────────────────
function darkenHex(hex, amount = 30) {
  const h = hex.replace('#', '');
  const r = Math.max(0, parseInt(h.slice(0,2),16) - amount);
  const g = Math.max(0, parseInt(h.slice(2,4),16) - amount);
  const b = Math.max(0, parseInt(h.slice(4,6),16) - amount);
  return '#' + [r,g,b].map(x => x.toString(16).padStart(2,'0')).join('');
}

// ─── Colorize HTML & Layout Centering ────────────────────────
function colorizeHTML(html, hex, useGradient) {
  const dark = darkenHex(hex, 35);
  let out = html;

  // Ensure THE SIMPLE MEDIA CO., LTD stays black (#111827)
  out = out.replace(/(<span[^>]*style="[^"]*color:\s*)#[0-9a-fA-F]{3,6}([^"]*"[^>]*>\s*THE SIMPLE MEDIA CO)/gi, '$1#111827$2');

  // Replace primary theme colors
  TICKET_PRIMARIES.forEach(c => {
    out = out.split(c).join(hex);
  });

  // Remove dashed line under Terminal row directly from HTML
  out = out.replace(/<tr\s+style="[^"]*border-top:\s*1px\s+dashed[^"]*"[^>]*>/gi, '<tr style="border-collapse: collapse;">');

  const headerBg = useGradient
    ? `linear-gradient(135deg, ${hex} 0%, ${dark} 100%)`
    : hex;

  const css = `
<style id="__brand__">
:root { --sp: ${hex}; --sp-dk: ${dark}; --sp-hdr: ${headerBg}; }
td.main-color, .main-color, .main-color > div { background: var(--sp-hdr) !important; }
table[style*="border: 1px solid"], tr[style*="border: 1px solid"],
td[style*="border: 1px solid"], td[style*="border:1px solid"] { border-color: var(--sp) !important; }
td[style*="border-right: 1px dashed"], td[style*="border-right:1px dashed"] { border-right-color: var(--sp) !important; }
#eTktHeader { background: transparent !important; }
#eTktHeader p, #eTktHeader span { color: #111827 !important; }

/* ─── Remove dashed line under Terminal row ─── */
tr[style*="border-top: 1px dashed"],
tr[style*="border-top:1px dashed"],
tr[style*="border-top: 1px dashed #cacaca"],
tr[style*="border-top:1px dashed #cacaca"],
*[style*="border-top: 1px dashed #cacaca"],
*[style*="border-top:1px dashed #cacaca"] {
  border-top: none !important;
}

/* ─── Remove boxes from Passenger Type, Gender, and DOB ─── */
.be-passenger-details {
  gap: 0 !important;
}
.be-passenger-details span,
.be-passenger-type,
.be-passenger-gender,
.be-passenger-dob {
  border: none !important;
  padding: 0 !important;
  border-radius: 0 !important;
  background: transparent !important;
}

/* ─── Perfect Centering & Padding Preservation in Preview ─── */
html {
  margin: 0 !important;
  padding: 0 !important;
  background: #fff !important;
}

body {
  margin: 0 !important;
  padding: 0 !important;
  background: #fff !important;
  display: flex !important;
  justify-content: center !important;
  align-items: flex-start !important;
  width: 100% !important;
  overflow-x: hidden !important;
  overflow-y: hidden !important;
}

.content-wrapper, .printable-area-item {
  width: 100% !important;
  display: flex !important;
  justify-content: center !important;
  margin: 0 auto !important;
  transform: none !important;
  transform-origin: top center !important;
}

/* Ensure the main 210mm ticket table is centered and retains 30px padding */
table[style*="210mm"] {
  margin: 0 auto !important;
  padding: 30px !important;
  box-sizing: border-box !important;
  max-width: 100% !important;
}

/* Strict Airline Logo Constraints: max 120x48px (protects layout) */
.be-airline-master-logo {
  max-height: 48px !important;
  max-width: 120px !important;
  width: auto !important;
  height: auto !important;
  object-fit: contain !important;
  display: block !important;
  margin: 0 auto !important;
}
</style>`;

  return out.includes('</head>')
    ? out.replace('</head>', css + '\n</head>')
    : css + out;
}

// ─── Embed External Images as Base64 ────────────────────────
function imgToDataUri(url) {
  return new Promise(resolve => {
    if (!url || url.startsWith('data:')) { resolve(null); return; }
    // If it is the Simple Travel logo, use embedded base64 directly
    if (url.includes('TheSimpleTravel') || url.includes('cmccdn.net')) {
      resolve(SIMPLE_TRAVEL_LOGO_BASE64);
      return;
    }
    const img = new Image();
    img.crossOrigin = 'anonymous';
    img.onload = () => {
      try {
        const c = document.createElement('canvas');
        c.width = img.naturalWidth;
        c.height = img.naturalHeight;
        c.getContext('2d').drawImage(img, 0, 0);
        resolve(c.toDataURL('image/png'));
      } catch { resolve(null); }
    };
    img.onerror = () => resolve(null);
    img.src = url;
    setTimeout(() => resolve(null), 5000);
  });
}

async function embedImages(html) {
  // Directly replace Simple Travel logo CDN url with local base64
  html = html.replace(/https?:\/\/l8uim6bskq7o\.cmccdn\.net\/[^"']+/gi, SIMPLE_TRAVEL_LOGO_BASE64);

  const rx = /<img[^>]+src=["']([^"']+)["']/gi;
  const urls = new Set();
  let m;
  while ((m = rx.exec(html)) !== null) {
    if (!m[1].startsWith('data:')) urls.add(m[1]);
  }
  for (const url of urls) {
    const dataUri = await imgToDataUri(url);
    if (dataUri) {
      html = html.split(url).join(dataUri);
    }
  }
  return html;
}

// ─── Auto Resize Iframe (Removes 3rd Scrollbar) ─────────────
function resizeIframe() {
  const iDoc = previewIframe.contentDocument || previewIframe.contentWindow.document;
  if (!iDoc || !iDoc.body) return;

  previewIframe.style.height = '300px';
  const bodyH = iDoc.body.scrollHeight || 0;
  const docH = iDoc.documentElement ? iDoc.documentElement.scrollHeight : 0;
  const newH = Math.max(bodyH, docH);
  if (newH > 100) {
    previewIframe.style.height = (newH + 20) + 'px';
  }
}

// ─── Apply Airline Logo & Name to Iframe ────────────────────
function applyAirlineLogoToIframe() {
  const iDoc = previewIframe.contentDocument || previewIframe.contentWindow.document;
  if (!iDoc) return;

  // Guarantee Simple Travel logo is base64
  const stLogo = iDoc.getElementById('eTktLogo');
  if (stLogo && (!stLogo.src || !stLogo.src.startsWith('data:'))) {
    stLogo.src = SIMPLE_TRAVEL_LOGO_BASE64;
  }

  const logoSrc = state.airlineLogo || state.originalAirlineLogo;
  if (logoSrc) {
    iDoc.querySelectorAll('.be-airline-master-logo').forEach(img => {
      img.src = logoSrc;
      img.style.maxHeight = '48px';
      img.style.maxWidth = '120px';
      img.style.width = 'auto';
      img.style.height = 'auto';
      img.style.objectFit = 'contain';
      img.style.display = 'block';
      img.style.margin = '0 auto';

      if (state.editMode) {
        img.title = 'Click để tải logo hãng bay mới';
        img.style.cursor = 'pointer';
        img.onclick = () => {
          panelAirline.scrollIntoView({ behavior: 'smooth', block: 'center' });
          airlineLogoInput.click();
        };
      } else {
        img.onclick = null;
        img.title = '';
      }
    });
  }

  if (state.airlineName) {
    iDoc.querySelectorAll('.be-airline-master').forEach(el => {
      el.textContent = state.airlineName;
    });
  }

  resizeIframe();
}

// ─── Clean Ticket Layout & Passenger Tags ───────────────────
function normalizePassengerDetailsAndLayout(root) {
  if (!root) return;

  // 1. Remove dashed line under Terminal row
  root.querySelectorAll('tr[style*="border-top: 1px dashed"], tr[style*="border-top:1px dashed"]').forEach(tr => {
    tr.style.borderTop = 'none';
  });

  // 2. Format passenger details: remove boxes, insert ' / ' between type and gender
  root.querySelectorAll('.be-passenger-details').forEach(container => {
    container.style.display = 'flex';
    container.style.alignItems = 'center';
    container.style.gap = '0';

    const pType = container.querySelector('.be-passenger-type');
    const pGender = container.querySelector('.be-passenger-gender');

    if (pType) {
      pType.style.border = 'none';
      pType.style.padding = '0';
      pType.style.borderRadius = '0';
      pType.style.background = 'transparent';
    }

    if (pGender) {
      pGender.style.border = 'none';
      pGender.style.padding = '0';
      pGender.style.borderRadius = '0';
      pGender.style.background = 'transparent';
    }

    const typeText = pType ? pType.textContent.trim() : '';
    const genderText = pGender ? pGender.textContent.trim() : '';

    let sep = container.querySelector('.be-passenger-sep');
    if (typeText && genderText) {
      if (!sep) {
        sep = document.createElement('span');
        sep.className = 'be-passenger-sep';
        sep.style.margin = '0 5px';
        sep.style.color = '#555555';
        sep.textContent = '/';
        container.insertBefore(sep, pGender);
      }
    } else if (sep) {
      sep.remove();
    }
  });
}


// ─── Extract Ticket Data for Mobile & QR ───────────────────────
function extractTicketData(doc) {
  if (!doc) return null;

  // 1. PNR
  const pnrEl = doc.getElementById('pnrCode') || doc.querySelector('.be-booking-code');
  const pnr = pnrEl ? pnrEl.textContent.trim() : (state.fileName.replace(/[^A-Z0-9]/gi, '').slice(0, 6) || 'TICKET');

  // 2. Airline
  const airline = state.airlineName || 'VIETJET AIR';

  // 3. Flights
  const flights = [];
  const fnRows = Array.from(doc.querySelectorAll('.be-flight-number'));
  const fcRows = Array.from(doc.querySelectorAll('.be-fare-class'));
  const hbRows = Array.from(doc.querySelectorAll('.be-hand-baggage'));
  const abRows = Array.from(doc.querySelectorAll('.be-allowance-baggage'));

  const stTimes = Array.from(doc.querySelectorAll('.be-start-time'));
  const scCodes = Array.from(doc.querySelectorAll('.be-start-code'));
  const sdDates = Array.from(doc.querySelectorAll('.be-start-date'));
  const etTimes = Array.from(doc.querySelectorAll('.be-end-time'));
  const edDates = Array.from(doc.querySelectorAll('.be-end-date'));

  const count = Math.max(fnRows.length, stTimes.length, 1);
  for (let i = 0; i < count; i++) {
    const fn = fnRows[i] && fnRows[i].children[1] ? fnRows[i].children[1].textContent.trim() : '';
    const fc = fcRows[i] && fcRows[i].children[1] ? fcRows[i].children[1].textContent.trim().replace(/^J\d+_/, '') : '';
    const hb = hbRows[i] && hbRows[i].children[1] ? hbRows[i].children[1].textContent.trim() : '';
    const ab = abRows[i] && abRows[i].children[1] ? abRows[i].children[1].textContent.trim() : '';

    const dt = stTimes[i] ? stTimes[i].textContent.trim() : '';
    let from = scCodes[i] ? scCodes[i].textContent.trim() : '';
    let to = '';

    if (etTimes[i]) {
      const arrTd = etTimes[i].closest('td');
      if (arrTd) {
        const span200 = arrTd.querySelector('span[style*="font-size: 200%"]') || arrTd.querySelector('.be-end-code');
        if (span200) {
          to = span200.textContent.trim();
        } else {
          const match = arrTd.textContent.match(/\b([A-Z]{3})\b/);
          if (match) to = match[1];
        }
      }
    }

    const dd = sdDates[i] ? sdDates[i].textContent.trim() : '';
    const at = etTimes[i] ? etTimes[i].textContent.trim() : '';
    const ad = edDates[i] ? edDates[i].textContent.trim() : '';

    flights.push({
      fn,
      fc,
      from,
      to,
      dt,
      dd,
      at,
      ad,
      hb,
      ab
    });
  }

  // 4. Passengers
  const passengers = [];
  const passRows = Array.from(doc.querySelectorAll('tr[id^="eTktPassengerInfo"]'));
  passRows.forEach(pr => {
    const nameEl = pr.querySelector('.be-passenger-name');
    const genderEl = pr.querySelector('.be-passenger-gender');
    const typeEl = pr.querySelector('.be-passenger-type');
    const ticketEl = pr.querySelector('.be-ticket-number strong') || pr.querySelector('[id^="eTktPassengerTicketNumber"] strong');

    const n = nameEl ? nameEl.textContent.trim() : '';
    const g = genderEl ? genderEl.textContent.trim() : '';
    const t = typeEl ? typeEl.textContent.trim() : '';
    const tk = ticketEl ? ticketEl.textContent.trim() : '';

    if (n) {
      passengers.push({ n, g, t, tk });
    }
  });

  return {
    p: pnr,
    a: airline,
    c: state.themeColor,
    g: state.useGradient,
    f: flights,
    px: passengers,
    h: '0768.188.224'
  };
}

function generateMobileTicketUrl(ticketData) {
  if (!ticketData) return '';
  const jsonStr = JSON.stringify(ticketData);
  const compressed = LZString.compressToEncodedURIComponent(jsonStr);
  const origin = window.location.origin && window.location.origin !== 'null' ? window.location.origin : '';
  const idx = window.location.pathname.lastIndexOf('/');
  const path = (idx !== -1 ? window.location.pathname.slice(0, idx) : '') + '/m.html';
  return (origin ? origin + path : 'm.html') + '#' + compressed;
}

function getQRCodeDataUrl(text, size = 180) {
  const tempDiv = document.createElement('div');
  new QRCode(tempDiv, {
    text: text,
    width: size,
    height: size,
    correctLevel: QRCode.CorrectLevel.M
  });
  const canvas = tempDiv.querySelector('canvas');
  if (canvas) return canvas.toDataURL('image/png');
  const img = tempDiv.querySelector('img');
  return img ? img.src : '';
}

function syncA4QRBadge(root) {
  if (!root) return;
  // Clean up any old footer QR row
  const oldFooterQR = root.getElementById ? root.getElementById('eTktQRRow') : root.querySelector('#eTktQRRow');
  if (oldFooterQR) oldFooterQR.remove();

  const pnrEl = root.getElementById ? root.getElementById('pnrCode') : root.querySelector('#pnrCode') || root.querySelector('.be-booking-code');
  if (!pnrEl) return;
  const pnrTd = pnrEl.closest('td');
  if (!pnrTd) return;
  const headerTr = pnrTd.closest('tr');
  if (!headerTr) return;

  const allTds = Array.from(headerTr.querySelectorAll('td'));
  // Locate airline TD (the one that is not pnrTd and not qrTd)
  let qrTd = headerTr.querySelector('#eTktHeaderQR');
  const airlineTd = allTds.find(td => td !== pnrTd && td !== qrTd);

  if (!state.showA4QR) {
    if (qrTd) qrTd.style.display = 'none';
    pnrTd.style.width = '50%';
    pnrTd.style.borderRight = '1px dashed var(--sp)';
    if (airlineTd) {
      airlineTd.style.width = '50%';
      airlineTd.style.borderRight = 'none';
    }
    return;
  }

  const iDoc = previewIframe.contentDocument || previewIframe.contentWindow.document;
  const data = extractTicketData(iDoc);
  if (!data) return;
  const url = generateMobileTicketUrl(data);
  const qrDataUrl = getQRCodeDataUrl(url, 160);

  if (!qrTd) {
    qrTd = document.createElement('td');
    qrTd.id = 'eTktHeaderQR';
    qrTd.innerHTML = 
      '<div style="display: flex; flex-direction: column; align-items: center; justify-content: center;">' +
        '<img class="a4-header-qr-img" src="' + qrDataUrl + '" style="width: 68px; height: 68px; display: block; margin: 0 auto 3px;" alt="QR Code">' +
        '<div style="font-size: 9.5px; color: #475569; font-weight: 600; line-height: 1.25; text-align: center;">Quét mã để xem vé online</div>' +
      '</div>';
    headerTr.insertBefore(qrTd, pnrTd);
  } else {
    // Ensure it is placed before pnrTd
    if (qrTd.nextElementSibling !== pnrTd) {
      headerTr.insertBefore(qrTd, pnrTd);
    }
    qrTd.style.display = '';
    const img = qrTd.querySelector('.a4-header-qr-img');
    if (img) img.src = qrDataUrl;
  }

  // Set 3 equal columns: Col 1 (QR: 33.33%) | Col 2 (PNR: 33.33%) | Col 3 (Airline: 33.33%)
  qrTd.style.width = '33.33%';
  qrTd.style.textAlign = 'center';
  qrTd.style.verticalAlign = 'middle';
  qrTd.style.padding = '8px 4px';
  qrTd.style.borderRight = '1px dashed var(--sp)';

  pnrTd.style.width = '33.33%';
  pnrTd.style.textAlign = 'center';
  pnrTd.style.verticalAlign = 'middle';
  pnrTd.style.padding = '8px 4px';
  pnrTd.style.borderRight = '1px dashed var(--sp)';

  if (airlineTd) {
    airlineTd.style.width = '33.33%';
    airlineTd.style.textAlign = 'center';
    airlineTd.style.verticalAlign = 'middle';
    airlineTd.style.padding = '8px 4px';
    airlineTd.style.borderRight = 'none';
  }
}

// ─── Modal Mobile & QR Logic ────────────────────────────────
function openMobileQRModal() {
  const iDoc = previewIframe.contentDocument || previewIframe.contentWindow.document;
  if (!iDoc || !iDoc.body) {
    showToast('Chưa có dữ liệu vé!', 'error');
    return;
  }

  const ticketData = extractTicketData(iDoc);
  state.ticketData = ticketData;
  state.mobileUrl = generateMobileTicketUrl(ticketData);
  saveTicketToHistory();

  mobileUrlInput.value = state.mobileUrl;

  qrCodeContainer.innerHTML = '';
  new QRCode(qrCodeContainer, {
    text: state.mobileUrl,
    width: 140,
    height: 140,
    correctLevel: QRCode.CorrectLevel.M
  });

  const comp = LZString.compressToEncodedURIComponent(JSON.stringify(ticketData));
  mobilePreviewIframe.src = 'm.html#' + comp;

  modalMobileQR.style.display = 'flex';
}

function closeMobileQRModal() {
  modalMobileQR.style.display = 'none';
}

function copyMobileUrl() {
  if (!state.mobileUrl) return;
  navigator.clipboard.writeText(state.mobileUrl).then(() => {
    showToast('📋 Đã sao chép link vé trực tuyến!', 'success');
  }).catch(() => {
    mobileUrlInput.select();
    document.execCommand('copy');
    showToast('📋 Đã sao chép link vé trực tuyến!', 'success');
  });
}

function downloadStandaloneQR() {
  const pnr = state.ticketData?.p || 'SimpleTravel';
  const url = state.mobileUrl;
  if (!url) {
    showToast('Chưa có dữ liệu để tạo mã QR', 'error');
    return;
  }

  showToast('⏳ Đang tạo ảnh mã QR Full HD...', '');

  // Generate large high-resolution 680x680 QR code
  const tempDiv = document.createElement('div');
  new QRCode(tempDiv, {
    text: url,
    width: 680,
    height: 680,
    correctLevel: QRCode.CorrectLevel.M
  });

  setTimeout(() => {
    const qrCanvas = tempDiv.querySelector('canvas');
    if (!qrCanvas) {
      showToast('Chưa tạo được mã QR', 'error');
      return;
    }

    const exportCanvas = document.createElement('canvas');
    exportCanvas.width = 1080;
    exportCanvas.height = 1260;
    const ctx = exportCanvas.getContext('2d');

    // Background white
    ctx.fillStyle = '#ffffff';
    ctx.fillRect(0, 0, 1080, 1260);

    const renderContents = () => {
      // Subtle separator line
      ctx.strokeStyle = '#E2E8F0';
      ctx.lineWidth = 2;
      ctx.beginPath();
      ctx.moveTo(100, 195);
      ctx.lineTo(980, 195);
      ctx.stroke();

      // 2. Draw QR Code in center
      ctx.drawImage(qrCanvas, 200, 230, 680, 680);

      // 3. PNR Text below QR (bold 52px)
      ctx.fillStyle = '#0F172A';
      ctx.font = 'bold 52px -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif';
      ctx.textAlign = 'center';
      ctx.fillText('Mã đặt chỗ: ' + pnr, 540, 1010);

      // 4. Footer: Orange background, large text matching PNR size (50px)
      const footerColor = state.themeColor || BRAND_DEFAULT;
      ctx.fillStyle = footerColor;
      ctx.fillRect(0, 1120, 1080, 140);

      ctx.fillStyle = '#FFFFFF';
      ctx.font = 'bold 48px -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif';
      ctx.textAlign = 'center';
      ctx.fillText('Hotline: 0768.188.224', 540, 1206);

      const link = document.createElement('a');
      link.download = 'QR_VeMayBay_' + pnr + '.png';
      link.href = exportCanvas.toDataURL('image/png');
      link.click();
      showToast('✅ Đã tải file ảnh mã QR Full HD!', 'success');
    };

    // Draw Top Header: Logo on the left + "VÉ ĐIỆN TỬ" text on the right (matching PNR size and color)
    const logoImg = new Image();
    logoImg.onload = () => {
      // Draw Logo on left side: enlarged width 230, height 153
      ctx.drawImage(logoImg, 100, 24, 230, 153);

      // Draw "VÉ ĐIỆN TỬ" text on right side: no border/label, pure black, 52px bold (matching PNR)
      ctx.fillStyle = '#0F172A';
      ctx.font = 'bold 52px -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif';
      ctx.textAlign = 'right';
      ctx.fillText('VÉ ĐIỆN TỬ', 980, 118);

      renderContents();
    };
    logoImg.onerror = () => {
      ctx.fillStyle = state.themeColor || BRAND_DEFAULT;
      ctx.font = 'bold 52px -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif';
      ctx.textAlign = 'left';
      ctx.fillText('SIMPLE TRAVEL', 100, 118);

      ctx.fillStyle = '#0F172A';
      ctx.font = 'bold 52px -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif';
      ctx.textAlign = 'right';
      ctx.fillText('VÉ ĐIỆN TỬ', 980, 118);

      renderContents();
    };
    logoImg.src = SIMPLE_TRAVEL_LOGO_BASE64;
  }, 80);
}

async function captureMobileCard(scale = 2.5) {
  const mDoc = mobilePreviewIframe.contentDocument || mobilePreviewIframe.contentWindow.document;
  if (!mDoc) throw new Error('Không truy cập được màn hình mobile');

  let card = mDoc.getElementById('ticket-card') || mDoc.getElementById('ticketCard') || mDoc.querySelector('.ticket-card');
  if (!card) {
    await new Promise(r => setTimeout(r, 400));
    card = mDoc.getElementById('ticket-card') || mDoc.getElementById('ticketCard') || mDoc.querySelector('.ticket-card');
  }
  if (!card) throw new Error('Chưa tải xong vé');

  // Enforce standard mobile width (420px) during capture so it is never squished by iframe mockup
  const prevWidth = card.style.width;
  const prevMaxWidth = card.style.maxWidth;
  card.style.width = '420px';
  card.style.maxWidth = '420px';

  try {
    return await html2canvas(card, {
      scale: scale,
      useCORS: true,
      allowTaint: true,
      backgroundColor: '#FFFFFF',
      width: 420
    });
  } finally {
    card.style.width = prevWidth;
    card.style.maxWidth = prevMaxWidth;
  }
}

async function downloadMobileImage() {
  showLoading(true);
  try {
    const canvas = await captureMobileCard(3);
    const link = document.createElement('a');
    link.download = 'Ve_Mobile_' + (state.ticketData?.p || 'SimpleTravel') + '.png';
    link.href = canvas.toDataURL('image/png');
    link.click();
    showToast('✅ Đã tải ảnh vé Full HD thành công!', 'success');
  } catch (err) {
    console.error(err);
    showToast('❌ Lỗi tải ảnh: ' + err.message, 'error');
  } finally {
    showLoading(false);
  }
}

async function copyMobileImage() {
  showLoading(true);
  try {
    const canvas = await captureMobileCard(2.5);
    canvas.toBlob(async blob => {
      try {
        await navigator.clipboard.write([new ClipboardItem({ 'image/png': blob })]);
        showToast('📋 Đã copy ảnh vé vào clipboard!', 'success');
      } catch (e) {
        showToast('Trình duyệt không hỗ trợ copy ảnh trực tiếp', 'error');
      }
    });
  } catch (err) {
    console.error(err);
    showToast('❌ Lỗi copy ảnh: ' + err.message, 'error');
  } finally {
    showLoading(false);
  }
}

function copyFlightSummary() {
  const iDoc = previewIframe.contentDocument || previewIframe.contentWindow.document;
  const data = state.ticketData || (iDoc ? extractTicketData(iDoc) : null);
  if (!data) {
    showToast('Chưa có thông tin vé!', 'error');
    return;
  }

  let txt = '✈️ VÉ ĐIỆN TỬ - SIMPLE TRAVEL\n';
  txt += 'Mã đặt chỗ: ' + (data.p || '') + '\n';
  txt += 'Hãng: ' + (data.a || '') + '\n\n';

  (data.f || []).forEach(f => {
    txt += '🛫 ' + (f.dd || '') + '\n';
    txt += '   ' + (f.fn || '') + ' ' + (f.from || '') + ' - ' + (f.to || '') + ' ' + (f.dt || '') + ' ' + (f.at || '');
    if (f.ad && f.dd && f.ad !== f.dd) {
      txt += ' (+1 ngày: ' + f.ad + ')';
    }
    txt += '\n\n';
  });

  txt += 'Khách bay:\n';
  (data.px || []).forEach((p, idx) => {
    const tag = (p.t || '') + (p.t && p.g ? ' / ' : '') + (p.g || '');
    txt += (idx + 1) + '. ' + p.n + (tag ? ' (' + tag + ')' : '') + '\n';
  });

  navigator.clipboard.writeText(txt.trim()).then(() => {
    showToast('📋 Đã sao chép tóm tắt hành trình gửi khách!', 'success');
  }).catch(() => {
    showToast('⚠️ Không thể sao chép tự động', 'error');
  });
}

// ─── Render Preview ─────────────────────────────────────────
function updatePreview() {
  if (!state.rawHTML) return;

  let html = colorizeHTML(state.rawHTML, state.themeColor, state.useGradient);

  // Font injection
  if (state.fontFamily) {
    const fontName = state.fontFamily.replace(/ /g, '+');
    const fontLink = `<link href="https://fonts.googleapis.com/css2?family=${fontName}:wght@300;400;500;600;700&display=swap" rel="stylesheet">`;
    const fontStyle = `<style id="__font__">*{font-family:'${state.fontFamily}',sans-serif!important;}</style>`;
    html = html.replace('</head>', fontLink + fontStyle + '\n</head>');
  }

  // Font scale
  const scaleCSS = `<style id="__scale__">body{font-size:${state.fontScale}%!important;}</style>`;
  html = html.replace('</head>', scaleCSS + '\n</head>');

  const doc = previewIframe.contentDocument || previewIframe.contentWindow.document;
  doc.open();
  doc.write(html);
  doc.close();

  // Apply customizations & resize after rendering
  setTimeout(() => {
    normalizePassengerDetailsAndLayout(doc);
    applyAirlineLogoToIframe();
    syncLuuYToIframe();
    syncA4QRBadge(doc);
    resizeIframe();
  }, 60);

  setTimeout(resizeIframe, 300);
  setTimeout(resizeIframe, 800);
}

// ─── Apply Color ────────────────────────────────────────────
function applyColor(hex) {
  state.themeColor = hex;
  colorPicker.value = hex;
  colorHex.value = hex;
  markActivePreset(hex);
  updatePreview();
}

// ─── File Handling ──────────────────────────────────────────
function formatBytes(b) {
  if (b < 1024) return b + ' B';
  if (b < 1048576) return (b/1024).toFixed(1) + ' KB';
  return (b/1048576).toFixed(1) + ' MB';
}

async function loadFile(file) {
  if (!file || !file.name.match(/\.html?$/i)) {
    showToast('⚠️ Vui lòng chọn file HTML', 'error');
    return;
  }

  state.fileName = file.name;
  fileNameEl.textContent = file.name;
  fileSizeEl.textContent = formatBytes(file.size);

  showLoading(true);

  const reader = new FileReader();
  reader.onload = async e => {
    let html = e.target.result;
    try {
      html = await embedImages(html);
    } catch (err) {
      console.warn('Image embedding failed:', err);
    }
    state.rawHTML = html;

    // Extract original airline logo & name from ticket HTML
    const parser = new DOMParser();
    const doc = parser.parseFromString(html, 'text/html');
    const origLogoEl = doc.querySelector('.be-airline-master-logo');
    state.originalAirlineLogo = origLogoEl ? origLogoEl.getAttribute('src') : null;

    const origNameEl = doc.querySelector('.be-airline-master');
    state.originalAirlineName = origNameEl ? origNameEl.textContent.trim() : 'VIETJET AIR';
    state.airlineName = state.originalAirlineName;
    airlineNameInput.value = state.originalAirlineName || '';

    // Reset logo custom upload
    state.airlineLogo = null;
    airlineLogoPreviewRow.style.display = 'none';

    // Extract LƯU Ý
    state.luuYOriginal = extractLuuY(state.rawHTML);
    if (state.luuYContent) {
      luuYEditor.innerHTML = state.luuYContent;
    } else {
      state.luuYContent = state.luuYOriginal;
      luuYEditor.innerHTML = state.luuYOriginal || '';
    }

    renderLuuYTemplates();

    // Show panels
    fileInfo.style.display = 'flex';
    panelColors.style.display = '';
    panelFont.style.display = '';
    panelAirline.style.display = '';
    panelLuuY.style.display = '';
    previewEmpty.style.display = 'none';
    previewToolbar.style.display = 'flex';
    previewContainer.style.display = 'flex';

    applyColor(state.themeColor);
    showLoading(false);
    showToast('✅ Đã tải file thành công!', 'success');
    setTimeout(() => saveTicketToHistory(), 600);
  };
  reader.readAsText(file, 'UTF-8');
}

// ─── LƯU Ý Management & Templates ────────────────────────────
function extractLuuY(html) {
  const m = html.match(/id=["']eTktRemark["'][^>]*>([\s\S]*?)<\/td>/i);
  return m ? m[1].trim() : '';
}

function syncLuuYToIframe() {
  const iDoc = previewIframe.contentDocument || previewIframe.contentWindow.document;
  const el = iDoc.getElementById('eTktRemark');
  if (!el) return;
  if (!state.luuYVisible) {
    el.style.display = 'none';
  } else {
    el.style.display = '';
    if (state.luuYContent !== null) el.innerHTML = state.luuYContent;
  }
  resizeIframe();
}

function getStoredTemplates() {
  try {
    const raw = localStorage.getItem(LS_KEY_TEMPLATES);
    if (raw) return JSON.parse(raw);
  } catch(e) {}
  return DEFAULT_LUUY_TEMPLATES;
}

function saveTemplatesToStorage(templates) {
  try {
    localStorage.setItem(LS_KEY_TEMPLATES, JSON.stringify(templates));
  } catch(e) {}
}

function renderLuuYTemplates() {
  const templates = getStoredTemplates();
  luuYTemplateList.innerHTML = '';

  const activeT = templates.find(t => t.id === state.activeTemplateId);
  if (activeT && btnLuuYUpdateTemplate) {
    btnLuuYUpdateTemplate.style.display = 'inline-flex';
    btnLuuYUpdateTemplate.textContent = `💾 Lưu vào: ${activeT.name.slice(0, 14)}...`;
    btnLuuYUpdateTemplate.title = `Lưu các nội dung vừa chỉnh sửa đè vào mẫu "${activeT.name}"`;
  } else if (btnLuuYUpdateTemplate) {
    btnLuuYUpdateTemplate.style.display = 'none';
  }

  templates.forEach(t => {
    const isActive = (state.activeTemplateId === t.id);
    const item = document.createElement('div');
    item.className = 'luu-y-template-item' + (isActive ? ' active' : '');

    const nameSpan = document.createElement('span');
    nameSpan.className = 'luu-y-template-name';
    nameSpan.innerHTML = t.name + (isActive ? ' <span style="font-size:10px;color:#059669;">(Đang chọn)</span>' : '');
    nameSpan.title = 'Click để áp dụng mẫu này vào vé';
    nameSpan.addEventListener('click', () => applyTemplate(t));

    const actions = document.createElement('div');
    actions.className = 'luu-y-template-actions';

    // Button to apply
    const btnUse = document.createElement('button');
    btnUse.className = 'btn ' + (isActive ? 'btn--primary' : 'btn--outline') + ' btn--sm';
    btnUse.style.padding = '2px 6px';
    btnUse.style.fontSize = '10.5px';
    btnUse.textContent = isActive ? '✓ Đang dùng' : 'Dùng';
    btnUse.addEventListener('click', () => applyTemplate(t));
    actions.appendChild(btnUse);

    // Button to save edits directly to this template
    const btnSaveHere = document.createElement('button');
    btnSaveHere.className = 'btn btn--outline btn--sm';
    btnSaveHere.style.padding = '2px 5px';
    btnSaveHere.style.fontSize = '10.5px';
    btnSaveHere.title = `Lưu nội dung đang soạn vào mẫu "${t.name}"`;
    btnSaveHere.textContent = '💾 Lưu đè';
    btnSaveHere.addEventListener('click', (e) => {
      e.stopPropagation();
      updateTemplateContent(t.id);
    });
    actions.appendChild(btnSaveHere);

    // Delete button for custom templates
    if (!t.isDefault) {
      const btnDel = document.createElement('button');
      btnDel.className = 'btn btn--outline btn--sm';
      btnDel.style.padding = '2px 5px';
      btnDel.style.fontSize = '10px';
      btnDel.style.color = '#dc2626';
      btnDel.title = 'Xóa mẫu này';
      btnDel.textContent = '🗑';
      btnDel.addEventListener('click', e => {
        e.stopPropagation();
        deleteTemplate(t.id);
      });
      actions.appendChild(btnDel);
    }

    item.appendChild(nameSpan);
    item.appendChild(actions);
    luuYTemplateList.appendChild(item);
  });
}

function applyTemplate(t) {
  state.activeTemplateId = t.id;
  state.luuYContent = t.content;
  luuYEditor.innerHTML = t.content;
  syncLuuYToIframe();
  renderLuuYTemplates();
  showToast(`📋 Đã chọn: ${t.name}. Bạn có thể chỉnh sửa rồi bấm "💾 Lưu đè" để cập nhật!`, 'success');
}

function updateTemplateContent(id) {
  const content = luuYEditor.innerHTML;
  if (!content.trim()) {
    showToast('⚠️ Nội dung trống, không thể lưu', 'error');
    return;
  }
  let templates = getStoredTemplates();
  const tpl = templates.find(t => t.id === id);
  if (tpl) {
    tpl.content = content;
    saveTemplatesToStorage(templates);
    state.activeTemplateId = id;
    state.luuYContent = content;
    syncLuuYToIframe();
    renderLuuYTemplates();
    showToast(`💾 Đã lưu thay đổi vào mẫu "${tpl.name}" thành công!`, 'success');
  }
}

function addCustomTemplate(name, content) {
  if (!name.trim()) {
    showToast('⚠️ Vui lòng nhập tên cho bản mẫu mới', 'error');
    return;
  }
  if (!content.trim()) {
    showToast('⚠️ Nội dung LƯU Ý đang trống', 'error');
    return;
  }

  const templates = getStoredTemplates();
  const newT = {
    id: 'custom_' + Date.now(),
    name: name.trim(),
    isDefault: false,
    content: content
  };
  templates.push(newT);
  saveTemplatesToStorage(templates);
  state.activeTemplateId = newT.id;
  luuYNewTemplateName.value = '';
  renderLuuYTemplates();
  showToast(`💾 Đã lưu mẫu mới: "${newT.name}"!`, 'success');
}

function deleteTemplate(id) {
  let templates = getStoredTemplates();
  templates = templates.filter(t => t.id !== id);
  saveTemplatesToStorage(templates);
  if (state.activeTemplateId === id) state.activeTemplateId = null;
  renderLuuYTemplates();
  showToast('🗑 Đã xóa bản mẫu', '');
}

function resetAllTemplatesToDefault() {
  if (confirm('Khôi phục 3 bản mẫu (Tiếng Việt, Tiếng Anh, Song ngữ) về nội dung mặc định ban đầu?')) {
    saveTemplatesToStorage(DEFAULT_LUUY_TEMPLATES);
    state.activeTemplateId = 'vi';
    applyTemplate(DEFAULT_LUUY_TEMPLATES[0]);
    showToast('↺ Đã khôi phục 3 mẫu gốc mặc định!', 'success');
  }
}

// ─── Export: PDF (A4 Margin 1:1:1:1 cm, Zoom 0.905, Centered) ───
function exportPDF() {
  const iDoc = previewIframe.contentDocument || previewIframe.contentWindow.document;
  if (!iDoc || !iDoc.body) { showToast('Chưa có vé!', 'error'); return; }

  // Guarantee Simple Travel logo is base64 in print window
  const stLogo = iDoc.getElementById('eTktLogo');
  if (stLogo && (!stLogo.src || !stLogo.src.startsWith('data:'))) {
    stLogo.src = SIMPLE_TRAVEL_LOGO_BASE64;
  }

  normalizePassengerDetailsAndLayout(iDoc);
  syncA4QRBadge(iDoc);

  let html = '<!DOCTYPE html>\n' + iDoc.documentElement.outerHTML;

  // Ensure Simple Travel logo URL is base64
  html = html.replace(/https?:\/\/l8uim6bskq7o\.cmccdn\.net\/[^"']+/gi, SIMPLE_TRAVEL_LOGO_BASE64);

  // Print CSS: A4 with 10mm (1cm) margin on all 4 sides.
  // Uses zoom: 0.905 on the 210mm table so it scales cleanly down to exactly 190mm
  // between the 10mm margins without distorting column widths or cutting borders!
  const printCSS = `<style id="__print__">
@page {
  size: A4 portrait;
  margin: 10mm 10mm 10mm 10mm; /* Margin 1:1:1:1 cm */
}
@media print {
  *, *::before, *::after {
    box-sizing: border-box !important;
  }
  html, body {
    width: 100% !important;
    margin: 0 !important;
    padding: 0 !important;
    background: #fff !important;
    -webkit-print-color-adjust: exact !important;
    print-color-adjust: exact !important;
    color-adjust: exact !important;
    overflow: visible !important;
  }
  .content-wrapper, .printable-area-item {
    width: 100% !important;
    margin: 0 auto !important;
    display: block !important;
    transform: none !important;
  }
  /* Proportional zoom scales 210mm cleanly into 190mm (210mm * 0.905 = 190mm) */
  table[style*="210mm"] {
    width: 210mm !important;
    zoom: 0.905 !important;
    margin: 0 auto !important;
    padding: 30px !important;
    box-sizing: border-box !important;
    page-break-inside: auto !important;
  }
  .be-airline-master-logo {
    max-height: 48px !important;
    max-width: 120px !important;
    width: auto !important;
    height: auto !important;
    object-fit: contain !important;
    display: block !important;
    margin: 0 auto !important;
  }
  /* Remove dashed line under Terminal and passenger boxes in print */
  tr[style*="border-top: 1px dashed"],
  tr[style*="border-top:1px dashed"],
  tr[style*="border-top: 1px dashed #cacaca"],
  tr[style*="border-top:1px dashed #cacaca"] {
    border-top: none !important;
  }
  .be-passenger-details {
    gap: 0 !important;
  }
  .be-passenger-details span,
  .be-passenger-type,
  .be-passenger-gender,
  .be-passenger-dob {
    border: none !important;
    padding: 0 !important;
    border-radius: 0 !important;
    background: transparent !important;
  }
  * {
    box-shadow: none !important;
  }
}
</style>`;

  html = html.replace('</head>', printCSS + '</head>');

  // Use invisible print iframe to avoid browser popup blockers completely!
  let printIframe = document.getElementById('__print_iframe__');
  if (printIframe) {
    printIframe.remove();
  }
  printIframe = document.createElement('iframe');
  printIframe.id = '__print_iframe__';
  printIframe.style.cssText = 'position:fixed;right:0;bottom:0;width:0;height:0;border:0;visibility:hidden;';
  document.body.appendChild(printIframe);

  const pDoc = printIframe.contentDocument || printIframe.contentWindow.document;
  pDoc.open();
  pDoc.write(html);
  pDoc.close();

  showToast('📄 Đang mở hộp thoại In / Lưu PDF...', 'info');

  setTimeout(() => {
    try {
      printIframe.contentWindow.focus();
      printIframe.contentWindow.print();
    } catch (err) {
      console.warn('Print iframe error, fallback to window.open:', err);
      const win = window.open('', '_blank');
      if (win) {
        win.document.write(html);
        win.document.close();
        setTimeout(() => { win.focus(); win.print(); }, 400);
      } else {
        showToast('⚠️ Vui lòng cho phép popup để in PDF', 'error');
      }
    }
  }, 400);
}

// ─── Export: Capture ticket to canvas (With Padding & Guaranteed Logo) ───
async function captureToCanvas() {
  const iDoc = previewIframe.contentDocument || previewIframe.contentWindow.document;
  const ticketTable = iDoc.querySelector('table[style*="210mm"]') || iDoc.body;

  // Offscreen container
  const container = document.createElement('div');
  container.style.cssText = 'position:fixed;left:-9999px;top:0;width:860px;background:#ffffff;margin:0;padding:0;display:flex;justify-content:center;';

  // Copy all stylesheets
  const styles = iDoc.querySelectorAll('style, link[rel="stylesheet"]');
  styles.forEach(s => container.appendChild(s.cloneNode(true)));

  // Card wrapper with clean 25px 30px padding so borders NEVER touch edges!
  const cardWrapper = document.createElement('div');
  cardWrapper.style.cssText = 'width: 850px; background: #ffffff; padding: 25px 30px; margin: 0 auto; box-sizing: border-box;';

  // Clone the ticket table
  const clone = ticketTable.cloneNode(true);
  normalizePassengerDetailsAndLayout(clone);
  syncA4QRBadge(clone);
  clone.style.margin = '0 auto';
  clone.style.width = '100%';
  clone.style.maxWidth = '790px';
  clone.style.boxSizing = 'border-box';
  clone.style.padding = '30px';

  // Guarantee Simple Travel logo is base64 inside clone
  const cloneLogo = clone.querySelector('#eTktLogo');
  if (cloneLogo) {
    cloneLogo.src = SIMPLE_TRAVEL_LOGO_BASE64;
  }

  // Also replace any remaining CDN URLs in images inside clone
  clone.querySelectorAll('img').forEach(img => {
    if (img.src && (img.src.includes('TheSimpleTravel') || img.src.includes('cmccdn.net'))) {
      img.src = SIMPLE_TRAVEL_LOGO_BASE64;
    }
  });

  // --- PASSENGER INFORMATION FIX: Prevent "nhảy chữ" & border clipping in html2canvas ---
  // 1. Align Service Table Columns (Ancillary, Seat, Bag, Meal...)
  const serviceRows = clone.querySelectorAll('tr[id^="eTktPassengerServiceInfo"]');
  serviceRows.forEach(row => {
    const cells = row.children;
    if (cells.length >= 3) {
      cells[0].style.width = '75px';
      cells[0].style.minWidth = '75px';
      cells[0].style.maxWidth = '75px';
      cells[0].style.whiteSpace = 'nowrap';
      cells[0].style.verticalAlign = 'middle';

      cells[1].style.width = 'auto';
      cells[1].style.paddingLeft = '6px';
      cells[1].style.verticalAlign = 'middle';

      cells[2].style.width = '95px';
      cells[2].style.minWidth = '95px';
      cells[2].style.maxWidth = '95px';
      cells[2].style.textAlign = 'center';
      cells[2].style.verticalAlign = 'middle';
    }
  });

  // 2. Fix Passenger Table & <tr> borders so html2canvas doesn't cut through the bottom row
  const passengerTables = clone.querySelectorAll('table[id^="eTktPassengerTable"]');
  passengerTables.forEach(pt => {
    pt.style.borderCollapse = 'collapse';
    pt.style.height = 'auto';
    pt.style.overflow = 'visible';
  });

  const passengerRows = clone.querySelectorAll('tr[id^="eTktPassengerInfo"]');
  passengerRows.forEach(pr => {
    const bColor = pr.style.borderColor || state.themeColor || '#F5A623';
    pr.style.border = 'none';
    Array.from(pr.children).forEach(td => {
      td.style.borderTop = `1px solid ${bColor}`;
      td.style.borderBottom = `1px solid ${bColor}`;
      td.style.height = 'auto';
      td.style.verticalAlign = 'middle';
    });
    if (pr.children[0]) pr.children[0].style.borderLeft = `1px solid ${bColor}`;
    if (pr.children[pr.children.length - 1]) pr.children[pr.children.length - 1].style.borderRight = `1px solid ${bColor}`;
  });

  // 3. Reset flexGrow on container cells in passenger table
  clone.querySelectorAll('td[style*="background-color: #eff6ff"]').forEach(td => {
    td.style.flexGrow = 'initial';
    td.style.height = 'auto';
    td.style.verticalAlign = 'middle';
  });

  // 4. FIX VERTICAL ALIGNMENT IN HEADERS & ROUTE BADGES (Prevents "align bottom" / sunken text in html2canvas)
  const activeColor = state.themeColor || '#F5A623';
  const darkColor = darkenHex(activeColor, 35);
  const badgeBg = state.useGradient
    ? 'linear-gradient(135deg, ' + activeColor + ' 0%, ' + darkColor + ' 100%)'
    : activeColor;

  // 4.1. Header bars (Flight headers & Passenger Information header)
  const headers = clone.querySelectorAll('.scale-title-render.main-color');
  headers.forEach(h => {
    const text = h.textContent.trim().toUpperCase();
    h.style.lineHeight = '1.2';
    h.style.verticalAlign = 'middle';
    if (text.includes('PASSENGER INFORMATION')) {
      // Passenger header bar (calibrated vertical center)
      h.style.paddingTop = '0px';
      h.style.paddingBottom = '10px';
      h.style.paddingLeft = '8px';
      h.style.paddingRight = '8px';
    } else {
      // Flight header bars (calibrated vertical center)
      h.style.paddingTop = '0px';
      h.style.paddingBottom = '18px';
      h.style.paddingLeft = '10px';
      h.style.paddingRight = '10px';
    }
  });

  // 4.2. Route badges (SGN-MEL, MEL-SGN) - Wrap badge text in vertically centered badge
  const routeBadges = clone.querySelectorAll('.be-service-route');
  routeBadges.forEach(rb => {
    const text = rb.textContent.trim();
    rb.className = 'be-service-route scale-render editable';
    rb.style.background = 'transparent';
    rb.style.padding = '0';
    rb.style.textAlign = 'right';
    rb.style.verticalAlign = 'middle';
    rb.innerHTML = '<span style="background:' + badgeBg + '; color:#ffffff; padding:0px 8px 8px 8px; border-radius:3px; text-align:center; display:inline-block; font-size:12px; font-weight:bold; line-height:1.1; vertical-align:middle;">' + text + '</span>';
  });

  cardWrapper.appendChild(clone);
  container.appendChild(cardWrapper);
  document.body.appendChild(container);

  // Wait for all images to ensure loaded
  const imgs = container.querySelectorAll('img');
  await Promise.all(Array.from(imgs).map(img => {
    if (img.complete && img.naturalWidth > 0) return Promise.resolve();
    return new Promise(r => { img.onload = r; img.onerror = r; setTimeout(r, 2000); });
  }));

  const canvas = await html2canvas(cardWrapper, {
    scale: 2,
    useCORS: true,
    allowTaint: true,
    backgroundColor: '#ffffff',
    logging: false,
    width: cardWrapper.scrollWidth,
    height: cardWrapper.scrollHeight,
    windowWidth: cardWrapper.scrollWidth,
    windowHeight: cardWrapper.scrollHeight,
  });

  document.body.removeChild(container);
  return canvas;
}

// ─── Export: PNG ─────────────────────────────────────────────
async function exportImage() {
  showLoading(true);
  try {
    const canvas = await captureToCanvas();
    const link = document.createElement('a');
    link.download = (state.fileName.replace(/\.html?$/i,'') || 'eticket') + '.png';
    link.href = canvas.toDataURL('image/png');
    link.click();
    showToast('✅ Đã tải ảnh PNG thành công (đầy đủ logo & khoảng đệm lề)!', 'success');
  } catch(err) {
    console.error(err);
    showToast('❌ Lỗi: ' + err.message, 'error');
  } finally {
    showLoading(false);
  }
}

// ─── Export: Copy to clipboard ──────────────────────────────
async function copyImage() {
  showLoading(true);
  try {
    const canvas = await captureToCanvas();
    canvas.toBlob(async blob => {
      try {
        await navigator.clipboard.write([new ClipboardItem({'image/png': blob})]);
        showToast('📋 Đã copy ảnh vào clipboard (đầy đủ logo & khoảng đệm lề)!', 'success');
      } catch {
        const url = URL.createObjectURL(blob);
        const a = document.createElement('a');
        a.href = url; a.download = 'eticket.png'; a.click();
        URL.revokeObjectURL(url);
        showToast('⬇️ Đã tải ảnh (clipboard không khả dụng)', '');
      }
      showLoading(false);
    }, 'image/png');
    return;
  } catch(err) {
    showToast('❌ Lỗi: ' + err.message, 'error');
  }
  showLoading(false);
}

// ─── Edit Mode ──────────────────────────────────────────────
function toggleEditMode() {
  state.editMode = !state.editMode;
  const iDoc = previewIframe.contentDocument || previewIframe.contentWindow.document;

  if (state.editMode) {
    iDoc.body.contentEditable = 'true';
    iDoc.body.style.outline = 'none';
    const s = iDoc.createElement('style');
    s.id = '__edit__';
    s.textContent = '[contenteditable="true"] *:hover{outline:1.5px dashed rgba(245,166,35,.7)!important;cursor:text;}';
    iDoc.head.appendChild(s);

    editToolbar.style.display = 'flex';
    editBanner.classList.add('visible');
    btnToggleEdit.classList.add('active');
    btnToggleEdit.querySelector('span').textContent = 'Tắt soạn thảo';

    applyAirlineLogoToIframe();
    showToast('✏️ Soạn thảo BẬT — click text để sửa, click logo để đổi hãng', '');
  } else {
    iDoc.body.contentEditable = 'false';
    const s = iDoc.getElementById('__edit__');
    if (s) s.remove();
    editToolbar.style.display = 'none';
    editBanner.classList.remove('visible');
    btnToggleEdit.classList.remove('active');
    btnToggleEdit.querySelector('span').textContent = 'Soạn thảo';

    applyAirlineLogoToIframe();
    showToast('💾 Đã lưu chỉnh sửa', 'success');
  }
  resizeIframe();
}

function execCmd(cmd) {
  const iDoc = previewIframe.contentDocument || previewIframe.contentWindow.document;
  iDoc.execCommand(cmd, false, null);
  iDoc.body.focus();
  resizeIframe();
}

// ─── Font Scale ─────────────────────────────────────────────
function setFontScale(val) {
  state.fontScale = Math.max(50, Math.min(150, val));
  fontSlider.value = state.fontScale;
  fontValue.textContent = state.fontScale + '%';
  const iDoc = previewIframe.contentDocument || previewIframe.contentWindow.document;
  let s = iDoc.getElementById('__scale__');
  if (!s) { s = iDoc.createElement('style'); s.id = '__scale__'; iDoc.head.appendChild(s); }
  s.textContent = `body{font-size:${state.fontScale}%!important;}`;
  setTimeout(resizeIframe, 50);
}

// ─── Font Family ────────────────────────────────────────────
function setFont(fontName) {
  state.fontFamily = fontName;
  updatePreview();
}

// ─── UI Helpers ─────────────────────────────────────────────
function showLoading(on) { loading.style.display = on ? 'flex' : 'none'; }

function showToast(msg, type = '') {
  const t = document.createElement('div');
  t.className = 'toast' + (type ? ' toast--' + type : '');
  t.textContent = msg;
  toastCont.appendChild(t);
  setTimeout(() => {
    t.classList.add('out');
    t.addEventListener('animationend', () => t.remove(), { once: true });
  }, 3000);
}

// ─── Event Listeners ────────────────────────────────────────

// Upload ticket
uploadZone.addEventListener('click', () => fileInput.click());
fileInput.addEventListener('change', e => { if (e.target.files[0]) loadFile(e.target.files[0]); });
uploadZone.addEventListener('dragover', e => { e.preventDefault(); uploadZone.classList.add('drag-over'); });
uploadZone.addEventListener('dragleave', () => uploadZone.classList.remove('drag-over'));
uploadZone.addEventListener('drop', e => {
  e.preventDefault(); uploadZone.classList.remove('drag-over');
  if (e.dataTransfer.files[0]) loadFile(e.dataTransfer.files[0]);
});

// Remove file
btnRemove.addEventListener('click', () => {
  state.rawHTML = null;
  state.luuYOriginal = null;
  state.luuYContent = null;
  state.airlineLogo = null;
  state.airlineName = null;
  fileInput.value = '';
  fileInfo.style.display = 'none';
  panelColors.style.display = 'none';
  panelFont.style.display = 'none';
  panelAirline.style.display = 'none';
  panelLuuY.style.display = 'none';
  previewEmpty.style.display = '';
  previewToolbar.style.display = 'none';
  previewContainer.style.display = 'none';
  if (state.editMode) {
    state.editMode = false;
    editToolbar.style.display = 'none';
    editBanner.classList.remove('visible');
    btnToggleEdit.classList.remove('active');
    btnToggleEdit.querySelector('span').textContent = 'Soạn thảo';
  }
});

// Colors
colorPicker.addEventListener('input', e => { colorHex.value = e.target.value; applyColor(e.target.value); });
colorHex.addEventListener('change', e => {
  let v = e.target.value.trim();
  if (!v.startsWith('#')) v = '#' + v;
  if (/^#[0-9a-fA-F]{6}$/.test(v)) { colorPicker.value = v; applyColor(v); }
});
colorHex.addEventListener('keydown', e => { if (e.key === 'Enter') colorHex.dispatchEvent(new Event('change')); });
gradientToggle.addEventListener('change', e => { state.useGradient = e.target.checked; updatePreview(); });
btnReset.addEventListener('click', () => applyColor(BRAND_DEFAULT));

// Font
fontSelect.addEventListener('change', e => setFont(e.target.value));

// ─── Airline Panel Events (Custom Upload Only) ──────────────
airlineUploadZone.addEventListener('click', () => airlineLogoInput.click());

airlineLogoInput.addEventListener('change', e => {
  const file = e.target.files[0];
  if (!file) return;
  if (!file.type.match(/^image\//i)) {
    showToast('⚠️ Vui lòng chọn file hình ảnh (.png, .jpg, .svg)', 'error');
    return;
  }
  const reader = new FileReader();
  reader.onload = ev => {
    state.airlineLogo = ev.target.result;
    airlineLogoPreviewImg.src = state.airlineLogo;
    airlineLogoNameDisplay.textContent = file.name;
    airlineLogoPreviewRow.style.display = 'flex';
    applyAirlineLogoToIframe();
    showToast('✅ Đã tải logo hãng! Kích thước tự động tối ưu tối đa 120×48px.', 'success');
  };
  reader.readAsDataURL(file);
});

btnAirlineClear.addEventListener('click', () => {
  state.airlineLogo = null;
  airlineLogoPreviewRow.style.display = 'none';
  airlineLogoInput.value = '';
  applyAirlineLogoToIframe();
  showToast('↺ Đã gỡ logo tùy chỉnh, quay về logo gốc', '');
});

btnAirlineApply.addEventListener('click', () => {
  state.airlineName = airlineNameInput.value.trim() || state.airlineName;
  applyAirlineLogoToIframe();
  showToast('✅ Đã cập nhật logo và tên hãng bay vào vé!', 'success');
});

btnAirlineReset.addEventListener('click', () => {
  state.airlineLogo = null;
  state.airlineName = state.originalAirlineName;
  airlineLogoPreviewRow.style.display = 'none';
  airlineLogoInput.value = '';
  airlineNameInput.value = state.originalAirlineName || '';
  applyAirlineLogoToIframe();
  showToast('↺ Đã khôi phục logo & tên hãng gốc', '');
});

if (btnEditToolbarAirline) {
  btnEditToolbarAirline.addEventListener('click', () => {
    panelAirline.scrollIntoView({ behavior: 'smooth', block: 'center' });
    panelAirline.style.transition = 'box-shadow 0.3s ease';
    panelAirline.style.boxShadow = '0 0 0 2px var(--brand-primary)';
    setTimeout(() => { panelAirline.style.boxShadow = ''; }, 1500);
  });
}

// ─── Export Buttons ─────────────────────────────────────────
btnPDF.addEventListener('click', exportPDF);
btnImg.addEventListener('click', exportImage);
btnCopy.addEventListener('click', copyImage);

// ─── Edit Toolbar ───────────────────────────────────────────
btnToggleEdit.addEventListener('click', toggleEditMode);
document.querySelectorAll('.edit-toolbar__btn[data-cmd]').forEach(btn => {
  btn.addEventListener('click', () => {
    if (btn.dataset.cmd === 'done') toggleEditMode();
    else execCmd(btn.dataset.cmd);
  });
});

// Font scale
fontSlider.addEventListener('input', e => setFontScale(+e.target.value));
btnFontDec.addEventListener('click', () => setFontScale(state.fontScale - 5));
fontInc.addEventListener('click', () => setFontScale(state.fontScale + 5));
btnFontReset.addEventListener('click', () => setFontScale(90));

// ─── LƯU Ý Rich Toolbar ─────────────────────────────────────
document.querySelectorAll('.luu-y-tb-btn[data-cmd]').forEach(btn => {
  btn.addEventListener('click', () => {
    const cmd = btn.dataset.cmd;
    const val = btn.dataset.val || null;
    if (cmd === 'color') {
      document.execCommand('foreColor', false, val);
    } else {
      document.execCommand(cmd, false, val);
    }
    luuYEditor.focus();
  });
});

// LƯU Ý Toggle
luuYToggle.addEventListener('change', e => {
  state.luuYVisible = e.target.checked;
  syncLuuYToIframe();
  showToast(state.luuYVisible ? '✅ Hiện LƯU Ý' : '🙈 Ẩn LƯU Ý', '');
});

btnLuuYApply.addEventListener('click', () => {
  state.luuYContent = luuYEditor.innerHTML;
  syncLuuYToIframe();
  showToast('✅ Đã áp dụng nội dung LƯU Ý vào vé!', 'success');
});

btnLuuYReset.addEventListener('click', () => {
  state.luuYContent = state.luuYOriginal;
  luuYEditor.innerHTML = state.luuYOriginal || '';
  state.activeTemplateId = null;
  renderLuuYTemplates();
  syncLuuYToIframe();
  showToast('↩️ Đã khôi phục nội dung gốc từ file', '');
});

// Update current active template button
if (btnLuuYUpdateTemplate) {
  btnLuuYUpdateTemplate.addEventListener('click', () => {
    if (state.activeTemplateId) {
      updateTemplateContent(state.activeTemplateId);
    }
  });
}

// Add New Template Button
btnLuuYAddTemplate.addEventListener('click', () => {
  const name = luuYNewTemplateName.value;
  const content = luuYEditor.innerHTML;
  addCustomTemplate(name, content);
});

luuYNewTemplateName.addEventListener('keydown', e => {
  if (e.key === 'Enter') {
    btnLuuYAddTemplate.click();
  }
});

if (btnLuuYResetAllTpls) {
  btnLuuYResetAllTpls.addEventListener('click', resetAllTemplatesToDefault);
}

// Window resize observer
window.addEventListener('resize', resizeIframe);


// ─── Mobile & QR Modal Listeners ────────────────────────────
if (btnMobileQR) {
  btnMobileQR.addEventListener('click', openMobileQRModal);
}
if (btnModalClose) {
  btnModalClose.addEventListener('click', closeMobileQRModal);
}
if (modalMobileQR) {
  modalMobileQR.addEventListener('click', e => {
    if (e.target === modalMobileQR) closeMobileQRModal();
  });
}
if (btnCopyMobileUrl) {
  btnCopyMobileUrl.addEventListener('click', copyMobileUrl);
}
if (btnDownloadQR) {
  btnDownloadQR.addEventListener('click', downloadStandaloneQR);
}
if (btnCopyFlightSummary) {
  btnCopyFlightSummary.addEventListener('click', copyFlightSummary);
}
if (btnDownloadMobileImg) {
  btnDownloadMobileImg.addEventListener('click', downloadMobileImage);
}
if (btnCopyMobileImg) {
  btnCopyMobileImg.addEventListener('click', copyMobileImage);
}
if (a4QRToggle) {
  a4QRToggle.addEventListener('change', e => {
    state.showA4QR = e.target.checked;
    const iDoc = previewIframe.contentDocument || previewIframe.contentWindow.document;
    syncA4QRBadge(iDoc);
    resizeIframe();
    showToast(state.showA4QR ? '✅ Đã bật mã QR trên vé A4' : '🙈 Đã tắt mã QR trên vé A4', '');
  });
}

// ─── Booking Code History Management ─────────────────────────
const LS_KEY_HISTORY = 'st_eticket_history_v1';

function getTicketHistory() {
  try {
    const raw = localStorage.getItem(LS_KEY_HISTORY);
    if (raw) return JSON.parse(raw);
  } catch (e) {
    console.warn('Failed to load history:', e);
  }
  return [];
}

function saveTicketToHistory() {
  if (!state.rawHTML) return;
  const iDoc = previewIframe.contentDocument || previewIframe.contentWindow.document;
  if (!iDoc) return;

  const data = extractTicketData(iDoc);
  if (!data || !data.p) return;

  const history = getTicketHistory();
  const pnr = data.p;

  const routesStr = (data.f || []).map(f => {
    let r = (f.from || '') + ' → ' + (f.to || '');
    if (f.dd) r += ' (' + f.dd + ')';
    return r;
  }).join(' • ');

  const passengersStr = (data.px || []).map(p => p.n).join(', ');

  const record = {
    pnr: pnr,
    airline: data.a || state.airlineName || 'VIETJET AIR',
    routes: routesStr,
    passengers: passengersStr,
    mobileUrl: state.mobileUrl || generateMobileTicketUrl(data),
    themeColor: state.themeColor,
    fontFamily: state.fontFamily,
    fontScale: state.fontScale,
    useGradient: state.useGradient,
    rawHTML: state.rawHTML,
    fileName: state.fileName || (pnr + '.html'),
    updatedAt: new Date().toLocaleString('vi-VN', {
      hour: '2-digit', minute: '2-digit', day: '2-digit', month: '2-digit', year: 'numeric'
    })
  };

  // Remove duplicate PNR if already exists
  const existingIdx = history.findIndex(item => item.pnr === pnr);
  if (existingIdx !== -1) {
    history.splice(existingIdx, 1);
  }

  // Prepend to top
  history.unshift(record);

  // Keep top 50
  if (history.length > 50) history.pop();

  try {
    localStorage.setItem(LS_KEY_HISTORY, JSON.stringify(history));
  } catch (e) {
    console.warn('LocalStorage save history error:', e);
  }
}

function openHistoryModal() {
  renderHistoryList();
  modalHistory.style.display = 'flex';
}

function closeHistoryModal() {
  modalHistory.style.display = 'none';
}

function renderHistoryList(query = '') {
  if (!historyTableContainer) return;
  const history = getTicketHistory();
  const q = query.trim().toLowerCase();

  const filtered = q ? history.filter(h => 
    (h.pnr || '').toLowerCase().includes(q) ||
    (h.airline || '').toLowerCase().includes(q) ||
    (h.routes || '').toLowerCase().includes(q) ||
    (h.passengers || '').toLowerCase().includes(q)
  ) : history;

  if (filtered.length === 0) {
    historyTableContainer.innerHTML = 
      '<div style="text-align: center; padding: 48px 20px; color: var(--brand-muted);">' +
        '<div style="font-size: 32px; margin-bottom: 8px;">📭</div>' +
        '<div style="font-weight: 600; font-size: 14px; color: var(--brand-dark);">' +
          (q ? 'Không tìm thấy kết quả nào phù hợp' : 'Chưa có vé nào được lưu trong lịch sử') +
        '</div>' +
        '<p style="font-size: 12px; margin-top: 4px;">Mỗi khi bạn tải vé lên hoặc thao tác, hệ thống sẽ tự động lưu lại tại đây.</p>' +
      '</div>';
    return;
  }

  let html = 
    '<table class="history-table">' +
      '<thead>' +
        '<tr>' +
          '<th style="width: 15%;">Mã đặt chỗ</th>' +
          '<th style="width: 18%;">Hãng bay</th>' +
          '<th style="width: 25%;">Hành trình</th>' +
          '<th style="width: 20%;">Khách bay</th>' +
          '<th style="width: 12%;">Thời gian</th>' +
          '<th style="width: 10%; text-align: center;">Thao tác</th>' +
        '</tr>' +
      '</thead>' +
      '<tbody>';

  filtered.forEach(item => {
    html += 
      '<tr>' +
        '<td><span class="history-pnr-badge">' + item.pnr + '</span></td>' +
        '<td><strong>' + item.airline + '</strong></td>' +
        '<td style="font-size: 11.5px; line-height: 1.35; color: #334155;">' + (item.routes || '—') + '</td>' +
        '<td style="font-size: 11.5px; line-height: 1.35; color: #475569;">' + (item.passengers || '—') + '</td>' +
        '<td style="font-size: 11px; color: #64748b;">' + item.updatedAt + '</td>' +
        '<td>' +
          '<div class="history-actions" style="justify-content: center;">' +
            '<button class="btn btn--primary btn--sm" data-action="load" data-pnr="' + item.pnr + '" title="Mở lại vé vào trình chỉnh sửa" style="padding: 4px 8px; font-size: 11px;">👁️ Mở</button>' +
            '<button class="btn btn--outline btn--sm" data-action="copy" data-pnr="' + item.pnr + '" title="Copy tóm tắt chuyến bay gửi Zalo" style="padding: 4px 8px; font-size: 11px;">📋</button>' +
            (item.mobileUrl ? '<a href="' + item.mobileUrl + '" target="_blank" class="btn btn--outline btn--sm" title="Mở vé Mobile" style="padding: 4px 8px; font-size: 11px; text-decoration: none;">📱</a>' : '') +
            '<button class="btn btn--outline btn--sm" data-action="delete" data-pnr="' + item.pnr + '" title="Xóa vé này khỏi lịch sử" style="padding: 4px 8px; font-size: 11px; color: #ef4444; border-color: #fca5a5;">🗑️</button>' +
          '</div>' +
        '</td>' +
      '</tr>';
  });

  html += '</tbody></table>';
  historyTableContainer.innerHTML = html;

  // Bind row action clicks
  historyTableContainer.querySelectorAll('button[data-action]').forEach(btn => {
    btn.addEventListener('click', () => {
      const action = btn.dataset.action;
      const pnr = btn.dataset.pnr;
      if (action === 'load') {
        loadTicketFromHistory(pnr);
      } else if (action === 'copy') {
        copyFlightSummaryByPnr(pnr);
      } else if (action === 'delete') {
        deleteHistoryItem(pnr);
      }
    });
  });
}

function loadTicketFromHistory(pnr) {
  const history = getTicketHistory();
  const item = history.find(h => h.pnr === pnr);
  if (!item || !item.rawHTML) {
    showToast('Không tìm thấy nội dung vé', 'error');
    return;
  }

  state.rawHTML = item.rawHTML;
  state.fileName = item.fileName || (pnr + '.html');
  fileNameEl.textContent = state.fileName;
  fileSizeEl.textContent = formatBytes(state.rawHTML.length);

  state.themeColor = item.themeColor || BRAND_DEFAULT;
  state.fontFamily = item.fontFamily || 'Inter';
  state.fontScale = item.fontScale || 90;
  state.useGradient = item.useGradient !== undefined ? item.useGradient : false;

  // Sync inputs
  if (colorPicker) colorPicker.value = state.themeColor;
  if (colorHex) colorHex.value = state.themeColor;
  if (fontSelect) fontSelect.value = state.fontFamily;
  if (fontSlider) fontSlider.value = state.fontScale;
  if (fontValue) fontValue.textContent = state.fontScale + '%';
  if (gradientToggle) gradientToggle.checked = state.useGradient;

  // Show UI
  fileInfo.style.display = 'flex';
  panelColors.style.display = '';
  panelFont.style.display = '';
  panelAirline.style.display = '';
  panelLuuY.style.display = '';
  previewEmpty.style.display = 'none';
  previewToolbar.style.display = 'flex';
  previewContainer.style.display = 'flex';

  applyColor(state.themeColor);
  closeHistoryModal();
  showToast('✅ Đã mở lại vé: ' + pnr, 'success');
}

function copyFlightSummaryByPnr(pnr) {
  const history = getTicketHistory();
  const item = history.find(h => h.pnr === pnr);
  if (!item) return;

  let txt = '✈️ VÉ ĐIỆN TỬ - SIMPLE TRAVEL\n';
  txt += 'Mã đặt chỗ: ' + item.pnr + '\n';
  txt += 'Hãng: ' + item.airline + '\n\n';
  if (item.routes) {
    txt += '🛫 Hành trình: ' + item.routes + '\n\n';
  }
  if (item.passengers) {
    txt += 'Khách bay:\n' + item.passengers.split(', ').map((n, i) => (i+1) + '. ' + n).join('\n') + '\n';
  }

  navigator.clipboard.writeText(txt.trim()).then(() => {
    showToast('📋 Đã copy tóm tắt vé ' + pnr, 'success');
  }).catch(() => {
    showToast('⚠️ Không thể sao chép tự động', 'error');
  });
}

function deleteHistoryItem(pnr) {
  let history = getTicketHistory();
  history = history.filter(h => h.pnr !== pnr);
  try {
    localStorage.setItem(LS_KEY_HISTORY, JSON.stringify(history));
  } catch (e) {}
  renderHistoryList(historySearchInput ? historySearchInput.value : '');
  showToast('🗑️ Đã xóa vé ' + pnr, '');
}

function clearAllHistory() {
  if (!confirm('Bạn có chắc chắn muốn xóa toàn bộ lịch sử vé đã lưu?')) return;
  try {
    localStorage.removeItem(LS_KEY_HISTORY);
  } catch (e) {}
  renderHistoryList();
  showToast('🧹 Đã xóa toàn bộ lịch sử vé', '');
}

// History Modal Listeners
if (btnHeaderHistory) {
  btnHeaderHistory.addEventListener('click', openHistoryModal);
}
if (btnToolbarHistory) {
  btnToolbarHistory.addEventListener('click', openHistoryModal);
}
if (btnModalHistoryClose) {
  btnModalHistoryClose.addEventListener('click', closeHistoryModal);
}
if (modalHistory) {
  modalHistory.addEventListener('click', e => {
    if (e.target === modalHistory) closeHistoryModal();
  });
}
if (historySearchInput) {
  historySearchInput.addEventListener('input', e => renderHistoryList(e.target.value));
}
if (btnClearAllHistory) {
  btnClearAllHistory.addEventListener('click', clearAllHistory);
}

// ─── Init ───────────────────────────────────────────────────
buildPresets();
renderLuuYTemplates();
